{
  "name": "project-launch",
  "description": "프로젝트 계획서 기반 Claude Code 설정 자동 생성. 계획서 작성(/project-launch:plan) → 고도화(/project-launch:refine) → 셋업(/project-launch:setup) → 구축(/project-launch:build) 4단계 워크플로우.",
  "author": {
    "name": "eyecool7"
  },
  "commands": "./commands/",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo ${CLAUDE_PLUGIN_ROOT} > /tmp/.project-launch-root"
          }
        ]
      }
    ]
  }
}