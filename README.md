# claude-code-project-setup

Claude Code 프로젝트 세팅 자동화 스킬. 계획서 하나로 CLAUDE.md + skills + agents + hooks를 생성한다.

## 한눈에 보기

| 순서 | 뭘 하나 | 어디서 | 참조 파일 |
|------|--------|--------|---------|
| **Step 1** | 프로젝트 계획서 작성 | claude.ai 채팅창 | `references/project-plan.md` |
| **Step 2** | 프로젝트 계획서 고도화 | Claude Code (Plan mode) | `references/plan-mode-prompt.md` |
| **Step 3** | 스킬 설치 | 내 컴퓨터 | (아래 설치 방법) |
| **Step 4** | 프로젝트 셋업 | Claude Code | `references/setup-prompt.md` |
| **Step 5** | 프로젝트 개발 시작 | Claude Code | — |

---

## Step 1: 프로젝트 계획서 작성 — claude.ai 채팅창

`references/project-plan.md` 파일의 내용을 복사한다. 상단의 **프로젝트 개요** 부분을 채우고 전송.

Claude가 인터뷰를 통해 디테일한 프로젝트 계획서를 완성한다.
파일명을 그대로 **project-plan.md**로 저장하고 프로젝트 폴더 루트에 넣는다.

### 계획서에 포함되는 내용

| 섹션 | 내용 | 필수 여부 |
|------|------|----------|
| 1. 제품 컨텍스트 | 한줄요약, 배경, 핵심기능+우선순위, 성공기준, 범위밖 | ✅ 필수 |
| 2. 워크플로우 | 사용자 플로우, LLM vs 스크립트 구분, 검증+실패처리 | ✅ 필수 |
| 3. 구현 설계 | 기술스택, 에이전트구조, 스킬목록, 에러전략, 의존성 | ✅ 필수 |
| 4. 기술 결정 | UI도구, 데이터흐름, URL구조, 외부서비스 | ⚪ 선택 (미정 허용) |
| 5. 구현 순서 | Phase별 의존관계 로드맵 | ✅ 필수 |

---

## Step 2: 프로젝트 계획서 고도화 — Claude Code (Plan mode)

Claude Code에서 프로젝트 폴더를 열고, `references/plan-mode-prompt.md`의 프롬프트를 입력한다.

Claude Code가 Step 1에서 만든 계획서를 읽고, 기술적인 관점에서 심층 인터뷰를 진행한다.
구현 방식, 트레이드오프, 우려 사항 등을 리뷰하여 계획서를 고도화시킨다.

---

## Step 3: 스킬 설치 — 내 컴퓨터

### 빠른 설치 (git clone)

```bash
cd my-project  # 프로젝트 폴더로 이동

# 이 레포를 임시로 클론
git clone https://github.com/YOUR_USERNAME/claude-code-project-setup.git /tmp/setup-skill

# 필요한 파일만 복사
cp /tmp/setup-skill/SKILL.md .
cp -r /tmp/setup-skill/.claude .
cp -r /tmp/setup-skill/references .
cp -r /tmp/setup-skill/scripts .

# 임시 클론 삭제
rm -rf /tmp/setup-skill
```

### 수동 설치 (zip 다운로드)

GitHub에서 zip을 다운로드하고, 아래 파일을 프로젝트 폴더에 복사:

```
레포 내용물                           →    my-project/ 에 넣기
───────────────────────────────         ──────────────────
SKILL.md                          →    my-project/SKILL.md
.claude/                          →    my-project/.claude/
references/                       →    my-project/references/
scripts/                          →    my-project/scripts/
```

### Step 3 완료 후 폴더 상태

```
my-project/
│
│  ── 세팅 스킬 (세팅 후 삭제됨) ──────────────────────
├── SKILL.md                     ← Claude Code가 읽는 세팅 지침서
├── references/
│   ├── claude-md-template.md    ← CLAUDE.md 생성 시 참조할 틀
│   ├── project-plan.md          ← 기획 프롬프트
│   ├── plan-mode-prompt.md      ← Plan mode 인터뷰 프롬프트
│   └── setup-prompt.md          ← 셋업 프롬프트
├── scripts/
│   ├── analyze-project.sh       ← 프로젝트 분석 자동화
│   └── validate-setup.sh        ← 세팅 결과 검증
│
│  ── .claude/ (템플릿 상태) ──────────────────────────
├── .claude/
│   ├── commands/
│   │   ├── check.md             ← /check 명령어
│   │   ├── commit-push-pr.md    ← /commit-push-pr 명령어
│   │   └── review.md            ← /review 명령어
│   ├── hooks/
│   │   ├── session-start.sh     ← 세션 시작 시 자동 실행
│   │   ├── edit-monitor.sh      ← 반복 수정 감지 (hint)
│   │   └── pre-commit-check.sh  ← ⚠️ 아직 {{변수}} 상태
│   ├── skills/                  ← ⚠️ 아직 TODO/{{변수}} 템플릿 상태
│   │   ├── conventions/SKILL.md
│   │   ├── dependencies/SKILL.md
│   │   ├── design-rules/SKILL.md
│   │   ├── easy-refactoring/SKILL.md
│   │   ├── error-handling/SKILL.md
│   │   ├── project-directory/SKILL.md
│   │   ├── security/SKILL.md
│   │   ├── skill-discovery/SKILL.md
│   │   └── testing/SKILL.md
│   ├── agents/
│   │   ├── test-runner.md       ← ⚠️ {{TEST_CMD}} 등 변수 상태
│   │   ├── code-reviewer.md
│   │   └── debugger.md
│   ├── lessons.md               ← 빈 템플릿 (개발 중 축적)
│   └── settings.json
│
│  ── 기획 산출물 (Step 1~2에서 작성) ─────────────────
└── project-plan.md
│
│  ── 아직 없는 것 ─────────────────────────────────────
│  (CLAUDE.md — Step 4에서 생성)
│  (.git — Step 4에서 초기화)
```

---

## Step 4: 프로젝트 셋업 — Claude Code

Claude Code에서 프로젝트 폴더를 열고, `references/setup-prompt.md`의 프롬프트를 입력한다.

### Step 4에서 일어나는 일

| 파일 | 변화 |
|------|------|
| **CLAUDE.md** | ⭐ **새로 생성** — 계획서 기반, 55줄 내외 |
| .claude/skills/error-handling | 📝 **TODO 채워짐** — 계획서 에러 전략 기반 |
| .claude/skills/security | 📝 **TODO 채워짐** — 계획서 인증/보안 모델 기반 |
| .claude/skills/testing | 📝 **TODO 채워짐** — 프로젝트 테스트 도구·mock 대상 |
| .claude/skills/project-directory | 📝 **TODO 채워짐** — 실제 디렉토리 구조 |
| .claude/skills/conventions | 📝 그대로 (추가 컨벤션 있으면 추가) |
| .claude/skills/easy-refactoring | 📝 그대로 |
| .claude/skills/skill-discovery | 📝 그대로 |
| .claude/skills/design-rules | 📝 **TODO 채워짐** — 프론트엔드 있을 때만 |
| .claude/skills/dependencies | 📝 **TODO 채워짐** — 의존성 gotcha 있을 때만 |
| .claude/agents/test-runner | 📝 **수정됨** — {{TEST_CMD}} 치환 |
| .claude/hooks/pre-commit-check.sh | 📝 **수정됨** — 프로젝트 검증 명령어로 |
| .claude/commands/check.md | 📝 **수정됨** — 패키지 매니저 명령어로 |
| .claude/settings.json | 📝 **수정됨** — 권한/hooks 설정 |
| .git/ | ⭐ **새로 생성** — git 초기화 + 첫 커밋 |
| SKILL.md | 🗑️ **삭제됨** |
| references/ | 🗑️ **삭제됨** |
| scripts/ | 🗑️ **삭제됨** |
| README.md | 🗑️ **삭제됨** |

---

## Step 5: 프로젝트 개발 시작 — Claude Code

셋업 컨텍스트가 개발을 오염시키지 않도록, 대화를 초기화한 뒤 시작한다.

```
/clear
```

그 후:

```
프로젝트 개발을 시작해
```

### Step 5 완료 후 폴더 상태

```
my-project/
├── CLAUDE.md                    ← ⭐ 55줄 내외. 매 세션 자동 로드.
├── .claude/
│   ├── commands/ (3)            ← /review, /check, /commit-push-pr
│   ├── hooks/ (3)               ← session-start, edit-monitor, pre-commit-check
│   ├── skills/ (7-9)            ← ⭐ Claude가 자동 발견. 맥락에 맞으면 로드.
│   │   ├── conventions/         ← 항상: 네이밍, import 순서, 타입 규칙
│   │   ├── error-handling/      ← 항상: 에러 처리 코드 쓸 때
│   │   ├── security/            ← 항상: 인증/보안 코드 쓸 때
│   │   ├── testing/             ← 항상: 테스트 작성할 때
│   │   ├── project-directory/   ← 항상: 파일/폴더 생성 위치 결정 시
│   │   ├── easy-refactoring/    ← 항상: 리팩토링 수행 시
│   │   ├── skill-discovery/     ← 항상: 외부 스킬 필요 시 자동 검색
│   │   ├── design-rules/        ← (프론트엔드) UI/스타일링 작업 시
│   │   └── dependencies/        ← (gotcha) 패키지 설치/설정 시
│   ├── agents/ (3)              ← ⭐ 필요 시 자동 위임. 독립 컨텍스트.
│   │   ├── test-runner.md
│   │   ├── code-reviewer.md
│   │   └── debugger.md
│   ├── lessons.md               ← 개발 중 실수/해결책 축적
│   └── settings.json
├── project-plan.md              ← 참조용 유지
└── .git/
```

**유용한 명령어:**
- `/check` — 타입체크 + 린트 + 테스트 한 줄 요약
- `/review` — 변경사항 코드 리뷰
- `/commit-push-pr` — 커밋 → 푸시 → PR 생성

**자동 작동:**
- **Skills (7-9개)** — 에러/보안/테스트/디자인 코드 쓸 때 관련 규칙 자동 로드
- **Agents (3개)** — 복잡한 테스트/리뷰/디버깅은 전담 에이전트가 독립 컨텍스트에서 처리
- **Lessons** — 실수 기록이 쌓이면 세션마다 알림, 같은 실수 방지
- **Skill Discovery** — 외부 스킬이 필요하면 자동 검색·제안 (설치 전 사용자 확인)

---

## 라이선스

MIT
